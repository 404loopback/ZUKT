# Plan de release (2 lots)

## Lot 1: refacto code + tests

- Suppression `internal/autopilot` et `internal/admin`.
- Contrat runtime strict MCP/search + fail-fast backend.
- Ajout MCP `get_status`.
- Renfort tests (startup health, tools MCP, config legacy).

## Lot 2: docs + scripts + migration

- Scripts indexation host + standardisation index partagé.
- Runbook incident backend down.
- Guide install locale prod.
- Guide migration Codex avant/après.
